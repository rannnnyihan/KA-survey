---
name: industry-evidence-dashboard
description: 地区行业多维证据库构建技能：对目标地区（默认 GB/T 4754 全部 70 个国民经济大类）的 21 项指标做全网公开数据检索、按分档标准逐项打分、对行业排序，并把行业归类进施耐德 NS 行业大类，最终生成带权重滑杆+右侧抽屉的单文件交互 HTML 证据库。当用户要求做某地区（上海/北京/深圳等）的行业分析、行业证据库、行业排名、行业景气对比，或要求刷新/合并/诊断已有证据库时使用。支持按用户指定的地区与时间检索最新数据。含 21 指标分档评分引擎、施耐德 NS 行业归类、HTML 生成器、CSV 四规则合并引擎与地区适配脚本，内置 MCP 连接器检查与用户引导流程。
agent_created: true
---

# Industry Evidence Dashboard（地区行业多维证据库）

## Overview

为任意地区构建「N 个细分行业 × 21 项指标」的逐格证据库，工作流分五段：

1. **全网公开数据检索**：对每个行业逐格采证，每格 = 数字/事实 + 年份 + 来源层级(S1–S4) + 具体来源链接，检索不到的如实标「缺{地区简称}数据（原因）」，宁缺勿假。
2. **按规则打分**：按《行业评分体系》21 项指标分档表（`references/scoring-standard-industry.md`）逐项打分，六大类：需求20 / 资本10 / 供给20 / 技术20 / 盈利20 / 政策10（合计100）。
3. **行业排序**：按总分降序排出 1–N 全榜（**按唯一国标行业代码排序**，同一行业归入多个 NS 组时共享同一排名，见「排名去重」）。
4. **NS 行业归类**：把国标行业归类进施耐德 NS 行业大类（37 组，tag 保/增/拓/其它仅标注、不参与排名），NS 行业分 = 成员国标行业总分均值。
5. **生成 HTML**：产出单文件交互 HTML（无外部依赖）——NS 两级主表（一级 NS 可展开出二级国标行业）、六大类权重滑杆（合计锁 100）、右侧抽屉（6 维度 21 指标分数 + 来源链接）、搜索、可折叠面板。**格式规格（列/抽屉/颜色/交互）固定，见 `references/html-format.md`**；换地区/时间只变地区词与数据内容，其余一律与首发版一致。

**核心目标**：同一套方法论 + 脚本，换地区、换时间即可复用。用户给定「地区 + 时间」，按当前时间检索该地区最新公开数据，重新生成 HTML。

**重要**：产出是「静态快照」——HTML 里每个数字都在生成那一刻检索固定，之后不会自动更新（不接自动取数流水线）。面向业务的功能说明见交付物根目录的 `功能说明.md`。

## Workflow（全流程）

### Step 0｜前置检查（MCP）

按 `references/mcp-connectors.md` 执行：采证主力是 WebSearch/WebFetch；若任务需要企业/金融数据（企查查、同花顺快查、天眼查、上奇产业通、Wind 等），先用 ToolSearch 确认对应 MCP 是否已连接。**未连接的，必须通过 suggest_plugin_install 弹卡片引导用户配置**（给出连接指引文案，用户跳过则改用公开检索降级并说明覆盖差异），禁止静默跳过或编造数据。

### Step 1｜确定地区、时间与行业清单

- 与用户确认三件事：
  - **地区**（如上海/北京/深圳，简称用于证据文本如"沪/京/深"）
  - **时间口径**：默认取**系统当前日期**，检索范围锁定「当前季度 + 上一季度」已发布的最新公开数据（用 `date` 命令取系统今天，再推算季度区间；用户没指定时间就用这个，勿用训练记忆里的旧日期）。
  - **行业范围**（默认 GB/T 4754 全部 70 个大类，代码为字符串如 "39"；用户可指定子集）
- 建工作目录：`WORKDIR`（中间产物）与 `OUTDIR`（交付物）。

### Step 2｜脚本已参数化，直接传参即可

核心脚本（rescore_ind21.py、gen_evidence_html.py）**不再硬编码地区/日期/路径**，通过命令行参数传入，无需改脚本或 sed 替换：

```bash
# 评分：<主库JSON> <地区简称>
python3 <skill_dir>/scripts/rescore_ind21.py WORKDIR/evidence_recs.json 京

# 生成 HTML：<主库JSON> <输出目录> <地区全名> <地区简称> <日期> <宏观指数JSON>
python3 <skill_dir>/scripts/gen_evidence_html.py WORKDIR/evidence_recs.json OUTDIR 北京 京 2026-09-03 WORKDIR/macro_indices.json
```

- 日期省略时自动用系统当天；宏观指数文件省略时默认找 `macro_indices.json`。
- `adapt_region.sh` 保留作备选（复制脚本到 WORKDIR），但已非必需；若行业数非 70，改 `gen_evidence_html.py` 顶部的地区默认值与统计口径即可。

### Step 3｜采证（并行子代理，检索最新时间数据）

按 `references/evidence-sources.md` 的检索配方与子代理 prompt 模板，派发多个采证子代理（每批 10–15 个行业），产出统一 CSV。硬性口径：

- **只收本地数据**，全国数据标「全国口径」（不计分）。
- **时效优先**：检索时用「当前时间 + 最新季度」关键词（如"2026 上半年 / 二季度 / 最新"），旧口径替换后入 hist。
- 缺格如实标「缺{简称}数据（原因）」。
- URL 必须是具体来源页，禁止官网首页占位。
- **禁止估计值**：每条证据的数字都必须可溯源到具体文章页，查不到就标缺数据，不许写"约+3.5%"这类无法溯源的估计数字（否则后续无法替换为具体链接）。
- 宏观指数（PMI/EPMI/PPI/FAI 等）单独写入 `WORKDIR/macro_indices.json`（对照展示，不评分）。

### Step 4｜建主库并合并

- 首建：按 `references/data-schema.md` 把采证结果组装成主库 JSON（`{地区}70行业指标证据_数据_{日期}.json`）。
- 增量合并：`python3 WORKDIR/merge_evidence_csv.py`（四规则 R1缺格创建 / R2 miss转正 / R3择新替换 / R4 hist补充；自动备份主库）。合并脚本顶部的 MAIN/CSVF 路径按需修改。

### Step 5｜评分（21 指标分档制）

```bash
python3 WORKDIR/rescore_ind21.py WORKDIR/evidence_recs.json {地区简称}
```

- 评分引擎按 21 指标分档表逐项打分（增速类 >30%/10–30%/0–10%/<0% 分档；定性类关键词分档；查不到按默认分），完整分档表见 `references/scoring-standard-industry.md`。
- 写出 `v21 = {total, cats, items, rank}`，其中 rank 为**唯一国标行业全榜排名**（1–70 不并列，同分按行业代码升序决胜）。
- 脚本读 `WORKDIR/evidence_recs.json` 写回。

### Step 6｜NS 行业归类 + 排序

- 主表按 `references/ns-industry-mapping.md` 的映射（37 组 NS 行业），把 70 个国标行业归类；同一国标行业可归入多个 NS 组（共 151 个成员实例）。
- NS 行业分 = 成员国标行业总分均值；NS 组间按均值排序、组内国标行业按总分排序。
- tag「保/增/拓/其它」仅作标注徽章，**不参与排名**。

### Step 7｜生成交付物

```bash
python3 WORKDIR/gen_evidence_html.py WORKDIR/evidence_recs.json OUTDIR {地区全名} {地区简称} {日期} WORKDIR/macro_indices.json
```

产出单文件 HTML（约 2.5MB，无外部依赖）：NS 两级主表（点击 NS 行展开二级国标行业、点行业行→右侧抽屉）、六大类权重滑杆（合计锁 100、其余类别按比例分配、恢复默认按钮）、搜索、分数筛选、可折叠维度面板、宏观指数对照面板、缺格/口径标注。

### Step 8｜验证（必做）

- JS 语法：提取 `<script>` 内容用 `node --check` 校验（f-string 双大括号转义易错，每次改生成器后必查）。
- 排名校验：确认唯一国标行业按总分排序、无重复占位（同一行业多组映射共享排名）。
- grep 抽查：行业卡数量、新证据关键词出现次数、旧区块零残留、`<output>`/被删列残留为 0、首页链接残留。
- 评分合理性：Top/Bottom 与常识对照；有疑问按「排名偏差诊断手册」排查。
- 最后用 present_files 交付 HTML（可顺带交付 `功能说明.md` 给业务方）。

## 常见迭代操作

| 需求 | 做法 |
|---|---|
| 补充新证据（CSV） | 四规则合并（Step 4）→ rescore_ind21 → 重新生成 HTML |
| 单行业排名偏低质疑 | 先看该行业 21 指标里哪些是默认分（查不到）→ 优先补稀疏指标证据，其次才考虑分档表微调（全量重算+前后对照，±10 分内变动为健康） |
| 链接是官网首页 | 取 ev 关键数字做 site: 检索替换为具体页；找不到保留并自动标「栏目首页」 |
| 刷新最新季度数据 | R3 择新替换，旧证据入 hist；宏观指数同步更新 macro_indices.json |
| 换地区 | adapt_region.sh 适配 → 重跑 Step 3–7 |

## Resources

### scripts/
- `rescore_ind21.py` — **现行评分引擎**：21 指标分档制（六大类 20/10/20/20/20/10），写 v21 与唯一全榜 rank。参数：`<主库JSON> <地区简称>`（默认 `evidence_recs.json 沪`）。读主库写回。
- `gen_evidence_html.py` — **现行 HTML 生成器**：NS 两级主表、六大类滑杆、右侧抽屉、排名去重（rankOf）、宏观映射全在此。参数：`<主库JSON> <输出目录> <地区全名> <地区简称> <日期> <宏观JSON>`（日期省略=系统当天，地区/简称/路径均已参数化，无硬编码）。
- `merge_evidence_csv.py` — CSV 四规则合并引擎（ALIAS 别名、TO_SUPP、T1→S1、hist 去重）。
- `adapt_region.sh` — 备选：复制脚本到工作目录（脚本已参数化，非必需）。
- `rescore_evidence.py` — 早期信号法评分引擎（**已弃用**，仅保留备查，勿用于新任务）。

### references/
- `scoring-standard-industry.md` — **现行评分标准**：21 指标分档表 + 口径红线（集团/全国口径不冒充本地、收入增长只取营收增速）。
- `ns-industry-mapping.md` — 施耐德 NS 行业 ↔ 国标行业映射（37 组）。
- `methodology.md` — 评分口径规范、排名偏差诊断手册、四规则、链接卫生。
- `data-schema.md` — 主库 JSON 结构、cell 字段、维度双轨命名、CSV 格式。
- `evidence-sources.md` — 采证检索配方、子代理 prompt 模板、补证专项数据源、质量红线。
- `html-format.md` — HTML 交付物格式规格（列/抽屉/颜色/交互的字段级约束，固定不改）。
- `mcp-connectors.md` — MCP 清单、连接检查流程、未连接时的弹窗引导规范。

## 关键经验（踩坑记录，务必遵守）

1. **评分用 21 指标分档制（rescore_ind21.py）**，不是旧的 19 指标信号法；《企业评分标准》23 指标是企业级，不适用于行业。
2. **集团/全国口径数字不得冒充本地口径计分**：如"龙头企业收入增长"用集团（上海医药 2835 亿全国营收）会虚高，须取本地制造口径（医药工业板块 245 亿）。ev 里集团总额标「不计入沪口径」。
3. **收入增长只取营业收入增速**，growth_vals 取峰值会把"归母净利增速"（华虹宏力 +385.9%）或"细分销售额增速"（君实药品销售 +40.32%）误当营收增速——取峰值前先复核口径。
4. **排名按唯一行业去重**：同一国标行业归入多个 NS 组会出现多个实例行，前端 recalc 必须按 data-code 去重排序（rankOf），否则高分行业重复占位把真实名次挤乱。
5. **企业注册热度/企业法人单位数**：用统计局《统计年鉴》「分行业企业法人单位数」表（一次覆盖全部行业大类，S1，与全国经济普查公报可交叉验证）；采矿业大类通常为空，如实标「无本地采矿业」。该数据滞后一年，如实标年份。
6. cells 的 dim 用 2 字名、类别用完整名，两套必须同步（DIM_CELL）。
7. f-string 写 JS 时所有字面 `{{ }}` 需双写，改完必跑 `node --check`。
8. `#list` 底部卡片区保留但 `display:none`——右侧抽屉从那里克隆内容，删 DOM 会坏抽屉。
9. 主库任何写操作前先备份（`cp` 到 WORKDIR），评分快照存 `rank_before.json` 供前后对照。
10. 产出是静态快照，不支持自动更新；要自动更新需另搭「自动取数→归类→打分→出报告」流水线（面向业务的说明见 `功能说明.md`）。
11. **格式/指标/权重固定，只变地区与时间**：HTML 结构、21 指标、六大类权重 20/10/20/20/20/10、评分分档、排名规则、NS 映射全部固定（见 `references/html-format.md` 第九节自查清单）；唯一可变是地区名/简称/日期/数据内容。换地区生成的 HTML 必须与首发版结构完全一致。
